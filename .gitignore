__pycache__/
*.egg-info/
*.pyc
dist/
build/
.pytest_cache/
*.bsp
.env

# Project intelligence (local only, not for public repo)
.claude/
context/
CLAUDE.md

# R&D experiments (internal)
experiments/

# Design system (internal)
design/

# Heavy data (benchmark results, visualizations)
data/

# MCP config (local only)
.mcp.json

# Banner source (large file, already committed once)
# banner.png is tracked — don't re-add if regenerated
